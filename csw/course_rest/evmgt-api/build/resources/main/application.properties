
# To have more than one api running at the same time, the
# server post can be changed.  At least this is one way to do it.

#server.port=8888

# This applicatgion properties file is very much like the  one
# used in the spring course.

spring.jpa.hibernate.ddl-auto=none

spring.datasource.name=eventmgt-dsrc
spring.datasource.url=jdbc:mysql://localhost:3306/stsevent
spring.datasource.username=root
spring.datasource.password=Dog4-J3ssi3

# The "name" of the api is evmgt-api.  Access is like:
# localhost:8080/evmgt-api/...

server.servlet.context-path=/evmgt-api

# Paging and sorting stuff.  For sorting, the
# client command is used:  ?sort=id,desc,sort=name,desc

spring.data.rest.default-page-size=30
